#!/usr/bin/env sh

if [ $# -eq 0 ]; then
    extensions_file="$HOME/.config/Code/User/extensions.txt"
else
    extensions_file="$1"
fi

xargs --max-lines=1 code --force --install-extension <"$extensions_file"
