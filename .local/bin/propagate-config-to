#!/usr/bin/env sh

[ -z "$1" ] && return
destination="$1"

ssh-copy-id "$destination"
scp $HOME/.ssh/authorized_keys "$destination:$HOME/.ssh/authorized_keys"
scp $HOME/.bashrc $HOME/.bash_aliases $HOME/.profile $HOME/.local/bin/.config-sshd "$destination":
